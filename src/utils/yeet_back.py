import time
from utils.watch import logger


def axe_catcher(streamlined_results, url_id):
    logger.debug(f'Bringing the Yeet Back for {url_id} !')
    logger.debug(f'Prepping {streamlined_results}')
    time.sleep(15)